spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  datasource:
    url: jdbc:mysql://localhost:3306/model_tests?serverTimezone=UTC
    username: root
    password: 1677613325
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  error:
    include-message: always
    include-binding-errors: always